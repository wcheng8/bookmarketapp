<h1>Editing Booklisting</h1>

<%= simple_form_for @booklisting do |f| %>
  <% if @booklisting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@booklisting.errors.count, "error") %> prohibited this booklisting from being saved:</h2>

      <ul>
      <% @booklisting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :picture %> <%= f.file_field :picture, accept:'image/png,image/gif,image/jpeg,image/jpg' %>
  <%= f.input :title %>
  <%= f.input :author %>
  <%= f.input :condition_id, collection: @conditions %>
  <%= f.input :price %>
  <%= f.input :description%>
  <%= f.button :submit %>
<% end %>


<%= link_to 'Show', @booklisting %> |
<%= link_to 'Back', booklistings_path %>
